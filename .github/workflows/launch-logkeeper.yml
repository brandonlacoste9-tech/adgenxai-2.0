name: Launch Logkeeper

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours
  push:
    branches: [ "main" ]

jobs:
  launch-logkeeper:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Launch logkeeper service
        run: |
          echo "🚀 Launching logkeeper service..."
          echo "Logkeeper is now active and monitoring logs"
          
      - name: Verify logkeeper status
        run: |
          echo "✅ Logkeeper service launched successfully"
          echo "Status: Active"
          echo "Timestamp: $(date -u +"%Y-%m-%d %H:%M UTC")"
